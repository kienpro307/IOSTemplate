# 🎯 CONTEXT FOR TASK

**Task**: Add dark mode toggle to Settings screen

**Generated**: 2025-11-23 17:36

---

## 📱 Project Overview

- **Name**: ios-template
- **Architecture**: TCA (The Composable Architecture) + Parameterized Component Pattern
- **UI Framework**: SwiftUI
- **iOS Target**: 16.0+
- **Language**: Swift 5.9+

### Key Architectural Pattern: Parameterized Component

This project uses a unique pattern where:
- **Components** = View + Config
- Views are pure rendering (NO hardcoded values)
- Configs contain all customization (data, callbacks, styling)
- Enables one template for multiple apps without forking

Example:
```swift
// Config
struct OnboardingConfig {
    let pages: [Page]
    let onComplete: () -> Void
}

// View
struct OnboardingView: View {
    let config: OnboardingConfig
    // Uses config for everything
}
```

---

## 📦 Relevant Modules

### Features (Relevance: 100%)

**Purpose**: UI features - Onboarding, Auth, Settings, Home, Profile

**Statistics**:
- Files: 11
- Lines of Code: 2,108

**Key Symbols**:
function:validateForm, struct:MainTabView, struct:ExploreView, struct:PermissionRow, struct:NotificationSettingsView, function:makeBody, function:handleResendEmail, function:completeOnboarding

**Dependencies**: ComposableArchitecture, SwiftUI, UIKit

**Main Files**:
- SettingsView.swift
- HomeView.swift
- RegistrationView.swift
- LoginView.swift
- ForgotPasswordView.swift
- ExploreView.swift

### Theme (Relevance: 79%)

**Purpose**: UI theming - Colors, Typography, Spacing, Component styles

**Statistics**:
- Files: 4
- Lines of Code: 668

**Key Symbols**:
enum:Grid, function:compactPadding, enum:Spacing, extension:Text, function:scaledFont, extension:Color, extension:EdgeInsets, struct:DestructiveButtonStyle

**Dependencies**: SwiftUI, UIKit

**Main Files**:
- Typography.swift
- Spacing.swift
- Colors.swift
- ButtonStyles.swift

### Core (Relevance: 30%)

**Purpose**: TCA foundation - State, Actions, Reducers, Store, ViewConfigs

**Statistics**:
- Files: 6
- Lines of Code: 619

**Key Symbols**:
function:handleNetworkAction, function:handleConfigAction, struct:MonetizationState, enum:ConnectionType, struct:AppReducer, enum:UserAction, enum:AppAction, enum:AppTab

**Dependencies**: ComposableArchitecture, Foundation, SwiftUI

**Main Files**:
- AppState.swift
- AppReducer.swift
- AppAction.swift
- AppStore.swift
- LoginConfig.swift
- OnboardingConfig.swift


---

## ⚠️ CRITICAL RULES


---

## ⚡ Quick Reference



---

## 💡 Instructions for Implementation

**Your Task**: Add dark mode toggle to Settings screen

**Follow These Guidelines**:

1. **TCA Architecture**: Use State → Action → Reducer → Effect pattern
2. **Parameterized Components**: NO hardcoded values in Views
   - Create Config object in `Core/ViewConfigs/`
   - Implement View in `Features/`
3. **SwiftUI Best Practices**: Use @ObservableState, proper state management
4. **Error Handling**: Add appropriate error handling and validation
5. **Testing**: Consider test coverage (target: 80%+)

**File Organization**:
- Configs → `Sources/iOSTemplate/Core/ViewConfigs/`
- Views → `Sources/iOSTemplate/Features/`
- Reducers → `Sources/iOSTemplate/Core/`
- Services → `Sources/iOSTemplate/Services/`

**Estimated Context Size**: ~600 tokens

---

*Context generated by Context Hub v1.0*
